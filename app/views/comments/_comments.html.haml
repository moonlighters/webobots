-# нужны локальнаы переменные:
-# * +commentable+ - комментируемый объект
-# * +comments+ - массив комментариев для отображения
%h3 Комментарии
.comments
  = render :partial => 'comments/form', :locals => {:commentable => commentable}
  - paginated_section comments, :param_name => :comments_page do
    = render comments

