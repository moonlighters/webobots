%li
  = link_to_match(match) + ':'
  = link_to_user match.first_version.firmware.user
  с прошивкой
  = link_to_firmware_version match.first_version
  %b
    против
  = link_to_user match.second_version.firmware.user
  с прошивкой
  = link_to_firmware_version match.second_version

